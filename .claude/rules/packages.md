# Package Management

When doing `npm install`:
- ALWAYS run in corresponding docker container IF running
- ALWAYS run on host machine
- ALWAYS run in storybook container IF installed to portfolio app
- ALWAYS restart Windsurf's TypeScript server after
