# Git Workflow

## Protected Branches
- `main`, `staging`, `develop` are protected
- Never commit directly to protected branches, branch out first

## Deployment
- Staging: PR from `develop` → `staging` (no direct push)
